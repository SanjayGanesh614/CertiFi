{
  "name": "contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network 0g-mainnet",
    "deploy-testnet": "npx hardhat run scripts/deploy.js --network 0g-testnet",
    "verify": "npx hardhat verify --network 0g-mainnet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/node": "^24.5.2",
    "ethers": "^6.15.0",
    "hardhat": "^2.26.0",
    "hardhat-deploy": "^1.0.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
